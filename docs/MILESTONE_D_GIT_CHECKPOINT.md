# Git Checkpoint Instructions

```bash
git checkout -b milestone-d-console-modeling-capture
git add .
git commit -m "Milestone D: add console modeling and capture framework"
```

Recommended follow-up commits:

```bash
git commit -am "Add test signal generation CLI"
git commit -am "Add capture session schema and feature extraction"
git commit -am "Add model fitting and profile validation scaffolds"
```