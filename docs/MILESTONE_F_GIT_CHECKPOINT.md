# Git Checkpoint Instructions

```bash
git checkout -b milestone-f-performance-runtime
git add .
git commit -m "Milestone F: add GPU/runtime scalability foundation"
```

Recommended follow-up commits:

```bash
git commit -am "Integrate lock-free telemetry into audio runtime"
git commit -am "Add realtime-safe memory pool patterns"
git commit -am "Add oversampling manager and performance profiling"
```