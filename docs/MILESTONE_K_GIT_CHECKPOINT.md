# Git Checkpoint Instructions

```bash
git checkout -b milestone-k-control-surface
git add .
git commit -m "Milestone K: add unified control-surface infrastructure"
```

Recommended follow-up commits:

```bash
git commit -am "Add virtual console strip management"
git commit -am "Add MIDI/OSC control mappings"
git commit -am "Add meter bridge and remote-session support"
```