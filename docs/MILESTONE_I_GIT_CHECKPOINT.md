# Git Checkpoint Instructions

```bash
git checkout -b milestone-i-commercialization
git add .
git commit -m "Milestone I: add commercialization and deployment infrastructure"
```

Recommended follow-up commits:

```bash
git commit -am "Add diagnostics and crash reporting"
git commit -am "Add preset package infrastructure"
git commit -am "Add portable session support"
```