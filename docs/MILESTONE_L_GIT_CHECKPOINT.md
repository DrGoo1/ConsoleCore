# Git Checkpoint Instructions

```bash
git checkout -b milestone-l-distributed-cluster
git add .
git commit -m "Milestone L: add distributed DSP cluster infrastructure"
```

Recommended follow-up commits:

```bash
git commit -am "Add DSP node topology management"
git commit -am "Add distributed render task infrastructure"
git commit -am "Add remote telemetry aggregation"
```