# Git Checkpoint Instructions

```bash
git checkout -b milestone-g-ai-profiling
git add .
git commit -m "Milestone G: add AI-assisted profiling infrastructure"
```

Recommended follow-up commits:

```bash
git commit -am "Add feature vector extraction hooks"
git commit -am "Add adaptive console matching scaffold"
git commit -am "Add dataset manager and telemetry capture"
```