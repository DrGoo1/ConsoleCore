# Git Checkpoint Instructions

```bash
git checkout -b milestone-j-hardware-measurement
git add .
git commit -m "Milestone J: add hardware capture and analog measurement infrastructure"
```

Recommended follow-up commits:

```bash
git commit -am "Add analog capture session management"
git commit -am "Add converter calibration profiles"
git commit -am "Add patch-chain and measurement-pass tracking"
```