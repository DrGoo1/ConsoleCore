# Git Checkpoint Instructions

```bash
git checkout -b integrate-tapecore-realtime-runtime
git add .
git commit -m "Add realtime TapeCore integration runtime"
```

Recommended follow-up commits:

```bash
git commit -am "Integrate per-stem tape runtime into JUCE callback"
git commit -am "Add shared tape telemetry aggregation"
git commit -am "Add Loopback routing integration"
```
