# Git Checkpoint Instructions

```bash
git checkout -b milestone-h-spatial-console
git add .
git commit -m "Milestone H: add immersive spatial console infrastructure"
```

Recommended follow-up commits:

```bash
git commit -am "Add immersive bus manager"
git commit -am "Add spatial crosstalk engine"
git commit -am "Add Atmos-style routing schemas"
```